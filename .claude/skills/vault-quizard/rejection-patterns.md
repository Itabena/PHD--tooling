# Rejection patterns

**This is the file the skill reads before generating questions — not the raw log.**
It is the short standing list of recurring failure modes distilled from
`rejections.jsonl`, the same move as the AI-notes review checklist on the
dashboard: a set of failure modes, not a list of every bad question. The raw log
is evidence to go back to; it never enters a generation prompt.

Refresh this list from the log periodically (the standard leaves "how often" open
until the log has ~20 entries, and leaves open whether distilling is a thing the
skill does or a thing done by hand — so for now it is a manual pass, not
automated). When you refresh, read the log, look for reasons that repeat, and
write each as one concrete rule below. Keep it short: if a pattern stops
recurring, drop it.

## Integrity failures to avoid

*(fabricated-quote and quote-unsupported rejections distill to here)*

- _None yet — the log is empty. Add patterns as they recur._

## Quality failures to avoid

*(trivia rejections distill to here)*

- _None yet — the log is empty. Add patterns as they recur._
